{
  "comment": "Azure DevOps Configuration - Copy to ~/.claude/ado-config.json",

  "_comment_required": "Only these 3 fields are required:",
  "organization": "your-organization",
  "project": "your-project",
  "repository": "your-repository",

  "_comment_optional": "Optional settings (uncomment to customize):",

  "_comment_paths": "Paths are auto-detected - only set if you need custom paths:",

  "_comment_token": "Token resolution order: env var > keychain > config file",
  "_comment_token_setup": "Recommended: export AZURE_DEVOPS_PAT='your-token' OR python scripts/token_manager.py --save",

  "debugMode": false,
  "autoFormatForClaudeCode": true
}
